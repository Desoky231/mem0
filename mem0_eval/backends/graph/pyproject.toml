[project]
name = "mem0-historical-graph-baseline"
version = "0.1.0"
description = "Isolated Mem0 v0.1.45 graph baseline"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastembed==0.8.0",
    "mem0ai[graph]==0.1.45",
    "nltk==3.9.2",
    "python-dotenv==1.2.2",
]

[tool.uv]
package = false
